// Calcule el área de un triangulo rectángulo que:
// Base 10 y altura 5

export function calculateAreaTriangle(base: number, height: number) {
  return (base * height) / 2;
}

console.log(calculateAreaTriangle(10, 5));
